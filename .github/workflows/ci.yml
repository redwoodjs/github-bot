name: ⚙️ CI

on:
  push:
    branches: [main]
  pull_request:

  workflow_dispatch:

jobs:
  redwood-project-ci:
    name: 🌲 Redwood CI
    runs-on: ubuntu-latest
    steps:
      - name: 🌲 Redwood CI
        uses: redwoodjs/project-ci-action@v0.1.1
